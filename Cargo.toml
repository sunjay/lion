[package]
name = "lion"
version = "0.1.0"
authors = ["Sunjay Varma <varma.sunjay@gmail.com>"]

[dependencies]
# Temporarily relying on someone's fork until nom 4.0 is published and this
# gets merged into nom_locate
nom_locate = { git = "https://github.com/CAD97/nom_locate", branch = "patch-1" }

[dependencies.nom]
version = "^4.0.0-beta2"
features = ["verbose-errors"]
